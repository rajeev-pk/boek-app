{% extends 'base.html.twig' %}

{% block title %}Filialen{% endblock %}

{% block body %}
    <h1>Filialen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ filialen.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ filialen.Naam }}</td>
            </tr>
            <tr>
                <th>Adres</th>
                <td>{{ filialen.Adres }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('filialen_index') }}">back to list</a>

    <a href="{{ path('filialen_edit', {'id': filialen.id}) }}">edit</a>

    {{ include('filialen/_delete_form.html.twig') }}
{% endblock %}
