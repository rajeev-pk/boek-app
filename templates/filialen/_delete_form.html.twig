<form method="post" action="{{ path('filialen_delete', {'id': filialen.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ filialen.id) }}">
    <button class="btn">Delete</button>
</form>
